<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mongo="http://www.springframework.org/schema/data/mongo"
	xsi:schemaLocation="http://www.springframework.org/schema/context
          http://www.springframework.org/schema/context/spring-context-3.1.xsd
          http://www.springframework.org/schema/data/mongo
          http://www.springframework.org/schema/data/mongo/spring-mongo-1.0.xsd
          http://www.springframework.org/schema/beans
          http://www.springframework.org/schema/beans/spring-beans-3.1.xsd">

	<mongo:mongo host="${mongo.host}" port="${mongo.port}" >
			<mongo:options 
		    connections-per-host="${mongo.connectionsPerHost}"
			threads-allowed-to-block-for-connection-multiplier="${mongo.threadsAllowedToBlockForConnectionMultiplier}"
			connect-timeout="${mongo.connectTimeout}"
            max-wait-time="${mongo.maxWaitTime}"
			auto-connect-retry="${mongo.autoConnectRetry}"
			socket-keep-alive="${mongo.socketKeepAlive}"
			socket-timeout="${mongo.socketTimeout}"
			slave-ok="${mongo.slaveOk}"
			write-number="${mongo.writeNumber}"
            write-timeout="${mongo.writeTimeout}"
            write-fsync="${mongo.writeFsync}" />
	</mongo:mongo>

	<mongo:db-factory dbname="sre-console"
	                  host="${mongo.host}"
	                  port="${mongo.port}"
	                  username="${mongo.username}"
	                  password="${mongo.password}"
	                  />
	<mongo:db-factory id='bi_result'
					  dbname="bi_result"
	                  host="${area.mongo.host}"
	                  port="${area.mongo.port}"
	                  username="${area.mongo.username}"
	                  password="${area.mongo.password}"
	                  />	                  
	
	<mongo:db-factory id='log_mongo'
					  dbname="access_logs"
	                  host="${area.mongo.host}"
	                  port="${area.mongo.port}"
	                  username="${area.mongo.username}"
	                  password="${area.mongo.password}"
	                  />
	                  
	<bean id="mongoTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
		<constructor-arg ref="mongoDbFactory" />
		<property name="writeResultChecking" value="EXCEPTION"/>
	</bean>
	
	<bean id="mongoTemplateBiResult" class="org.springframework.data.mongodb.core.MongoTemplate">
		<constructor-arg ref="bi_result" />
		<property name="writeResultChecking" value="EXCEPTION"/>
	</bean>
	
	
	<bean id="mongoLogTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
		<constructor-arg ref="log_mongo" />
		<property name="writeResultChecking" value="EXCEPTION"/>
	</bean>
	
	<mongo:mapping-converter base-package="com.yougou.dao.entity" />

</beans>